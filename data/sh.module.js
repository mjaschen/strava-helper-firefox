var StravaHelper = (function() {
    // root object of the Strava Helper plugin
    var sh = {};

    sh.config = {
        'logging': {
            'debug': true
        }
    };

    return sh;
}());